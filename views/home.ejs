<%- include ("partials/header")-%>

    <h1>Home</h1>

    <p> <%= StartingContent %> </p>



        <% posts.forEach(function(post){ %>

            <h1><%=post.titleBody%></h1>
                <p>
                    <% if (post && post.bodyPost) { %>
                        <%=post.bodyPost.substring(0, 100) + " ..."%>
                            <a href="/post/<%=post._id%>">Read More</a>
                            <% } %>
                            </p>

                            <% }) %>

                                <div>

                                    <a href="/compose">
                                        <button>+</button>
                                    </a>

                                </div>

                                <%- include ("partials/footer")-%>